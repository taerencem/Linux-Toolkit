# Linux Security Audit Script

This script performs a full security audit of a Linux system, checking users, groups, sudoers, SSH security, firewall rules, open ports, running services, Fail2Ban status, file permissions, and pending updates.

## Usage

```bash
bash audit-report.sh
