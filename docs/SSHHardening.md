# SSH Hardening Script

This script applies secure SSH configurations to protect Linux servers from unauthorized access. It disables root login, disables password authentication, enforces key-based login, and applies secure defaults.

## Usage

```bash
bash ssh-hardening.sh
