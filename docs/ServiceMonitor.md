# Linux Service Monitor Script

This script monitors a Linux service and automatically restarts it if it stops. It logs all events for auditing and troubleshooting.

## Usage

```bash
bash service-monitor.sh nginx
bash service-monitor.sh sshd
bash service-monitor.sh mysql
