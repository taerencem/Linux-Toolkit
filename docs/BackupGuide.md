# Linux Backup Script (tar + rsync)

This script creates compressed backups using tar and syncs them to a remote server using rsync.

## Usage

```bash
bash backup.sh /etc
bash backup.sh /var/www
bash backup.sh /home
