# Fail2Ban Setup Script

This script installs and configures Fail2Ban to protect Linux servers from brute-force attacks. It enables SSH protection, configures jail rules, and starts the Fail2Ban service.

## Usage

```bash
bash fail2ban-setup.sh
