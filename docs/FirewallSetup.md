# Linux Firewall Setup Script (UFW + firewalld)

This script configures a secure firewall depending on the Linux distribution.  
Supports both UFW (Ubuntu/Debian) and firewalld (CentOS/RHEL).

## Usage

```bash
bash firewall-setup.sh
